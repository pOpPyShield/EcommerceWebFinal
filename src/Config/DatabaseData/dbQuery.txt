//Query name, description, quantity, size of each product
SELECT Product.Name, Product.Description, Product.Price, Quantity.Quantity, Size.Size
FROM Product 
INNER JOIN Quantity
ON Product.IdProduct=Quantity.IdProduct 
INNER JOIN Size 
ON Quantity.IdSize=Size.IdSize
